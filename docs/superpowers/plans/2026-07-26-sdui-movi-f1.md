# SDUI Movi F1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** El Dashboard de movi se renderiza desde `screen_definitions` (editable por SQL sin deploy), con renderer tolerante, chrome nativo intacto y fallback byte-idéntico.

**Architecture:** Porta el patrón mergeado de NeoVita (`/Users/carolinarestrepo/Developer/NeoVita`, main — los implementadores PUEDEN leer esos archivos como referencia) con las adaptaciones del spec: sin cache persistente, secciones smart de finanzas (HERO_BALANCE/ACCOUNTS_SUMMARY), seed por-slug, 404 ante JSON corrupto, chrome (top bar + bottom nav) fuera del schema.

**Tech Stack:** Ktor/Exposed (harness H2 existente), kotlinx.serialization, Compose Multiplatform con el design system `Min*`.

**Spec:** `docs/superpowers/specs/2026-07-26-sdui-movi-design.md` (tipos, whitelist, seed y capas anti-rotura EXACTOS ahí — es la fuente de verdad de valores).

## Global Constraints

- Branch `feat/sdui-movi` en `/Users/carolinarestrepo/Developer/movi`. JBR 21 ya es JAVA_HOME. NO correr `./gradlew build` completo.
- Tipos de sección, whitelist de NAVIGATE (15 targets) y estructura del seed (5 secciones en el orden del spec): copiar VERBATIM del spec.
- Referencias NeoVita (leer, adaptar, NUNCA copiar paquetes): schema+tests `NeoVita/core/src/commonMain/kotlin/com/neovita/shared/network/dto/ScreenDto.kt` + su test; ruta/repo/seed `NeoVita/server/src/main/kotlin/com/neovita/server/{routes/ScreenRoutes.kt,db/repositories/ScreenRepository.kt,db/ScreenSeed.kt}`; renderer `NeoVita/shared/src/commonMain/kotlin/com/neovita/app/ui/sdui/`.
- Lecciones NeoVita OBLIGATORIAS: Json del cliente tolerante (verificar `ignoreUnknownKeys` en el HttpClient de movi `:core`; agregar si falta); `DashboardFallback` byte-idéntico sin refactor; chrome nativo fuera del schema; seedIfEmpty POR SLUG; sections_json corrupto → 404+warn.
- El repo de movi NO usa safeCall: `WalletRepositoryImpl` llama `client.get(...).body()` directo — para el 304 revisar el status ANTES de `.body()`.
- Cada tarea verde + commit. Harness HTTP = patrón `CreditRoutesTest.kt`.

## File Structure

```
core/.../shared/model/ScreenDefinition.kt            [C] DTOs + taxonomy + renderableSections (paquete model, junto a Finance.kt)
core/src/jvmTest/.../ScreenDefinitionTest.kt         [C] unit (source set de NoOpRepository)
core/.../shared/repository/WalletRepository(.Impl/Local/NoOp) [M] getScreen(slug, cachedVersion): ScreenDefinition?
server/.../db/Tables.kt                              [M] object Screens("screen_definitions")
server/.../db/DatabaseFactory.kt                     [M] registrar + seed
server/.../screens/ScreenSeed.kt                     [C] dashboard v1 (5 secciones del spec)
server/.../routes/ScreenRoutes.kt                    [C] GET /api/screens/{slug}
server/.../plugins/Routing.kt                        [M] registrar (dentro de authenticate)
server/test/.../routes/ScreenRoutesTest.kt           [C] HTTP (6 tests del spec)
shared/.../ui/sdui/SduiRenderer.kt                   [C] + secciones
shared/.../ui/dashboard/DashboardScreen.kt           [M] chrome + def→renderer | DashboardFallback
```

---

### Task 1: Schema + repo en `:core` (TDD)

**Files:** ver File Structure (4 archivos de repo + modelo + test).

**Interfaces (produce):** `ScreenDefinition(slug, version: Int, sections: List<ScreenSection>)`, `ScreenSection(type: String, title: String?=null, cards: List<ScreenCard> = emptyList(), text: String?=null)`, `ScreenCard(title, subtitle?=null, imageUrl?=null, badge?=null, action: ScreenAction?=null)`, `ScreenAction(type: String, target: String)` — todos @Serializable en `com.jvillada.movi.shared.model`; `object ScreenTaxonomy` con `SECTION_TYPES` (los 6 del spec), `ACTION_TYPES`, `NAVIGATE_TARGETS` (los 15 del spec); `fun renderableSections(def: ScreenDefinition): List<ScreenSection>` (misma lógica que NeoVita: filtra tipos, strippea acciones inválidas — NAVIGATE fuera de whitelist, OPEN_URL no-https, tipo desconocido); repo: `suspend fun getScreen(slug: String, cachedVersion: Int? = null): ScreenDefinition?` (Impl: `client.get("$baseUrl/api/screens/$slug") { cachedVersion?.let { header("If-None-Match", it.toString()) } }`, si `status == NotModified` → null, si no `.body()`; Local delega; NoOp devuelve null).

- [ ] **Step 1:** Test que falla — portar los 3 tests de `NeoVita/core/.../ScreenDtoTest.kt` (roundtrip / tipo desconocido filtrado / acciones strippeadas) adaptados a los nombres movi y sus targets (`"NAVIGATE","credits"` válida; `"NAVIGATE","settings"` inválida; sección desconocida `"HOLOGRAM_3D"`). Ubicación: `core/src/jvmTest/kotlin/com/jvillada/movi/shared/model/ScreenDefinitionTest.kt`. Run `./gradlew :core:jvmTest --tests "*.ScreenDefinitionTest"` → RED.
- [ ] **Step 2:** Implementar modelo+taxonomy+renderableSections (adaptar de NeoVita) + los 4 archivos de repo. Verificar el Json del HttpClient de movi (`grep -rn "ignoreUnknownKeys\|json(" core/src/commonMain/kotlin/com/jvillada/movi/data/` y donde se construya el client): si NO es tolerante, configurarlo `Json { ignoreUnknownKeys = true }` con comentario (lección NeoVita). → GREEN + `:core:jvmTest` completo + `:shared:compileDebugKotlinAndroid` (repo interface cambió — NoOp/Local al día).
- [ ] **Step 3:** Commit `feat(core): schema SDUI movi + getScreen con 304`.

---

### Task 2: Server — tabla, seed por-slug, ruta robusta (TDD HTTP)

**Interfaces (produce):** `object Screens : Table("screen_definitions")` (slug PK varchar(64), version Int, sectionsJson Text, active Bool default true, updatedAt Long); `SCREEN_SEED: List<ScreenDefinition>` en `server/.../screens/ScreenSeed.kt` — dashboard v1 con las 5 secciones del spec, transcribiendo el Dashboard actual (leer `shared/.../ui/dashboard/DashboardScreen.kt`): HERO_BALANCE → ACCOUNTS_SUMMARY → BANNER(title="Alertas", text="Sin alertas por ahora") → LINK_LIST(title="Explora", cards=[Inversiones→investments, Créditos→credits, Metas→goals, Suscripciones→subscriptions]) → BANNER(text="✦ Pregúntale a Movi AI", action NAVIGATE aichat); `seedScreens()` POR SLUG (inserta cada slug del seed que no exista; jamás actualiza existentes) llamado en `DatabaseFactory.init()`; `GET /api/screens/{slug}` (authenticate jwt): 200 DTO | 304 si If-None-Match==version | 404 si no existe, `!active`, O el sections_json no deserializa (con `log.warn`).

- [ ] **Step 1:** Test HTTP que falla — `ScreenRoutesTest.kt` con el harness de `CreditRoutesTest.kt` (H2 propia `screens_routes_test`, drop/create de TODAS las tablas + `Screens`, usuario A). 6 tests: `200 with seeded dashboard` (5 secciones, version 1, primera HERO_BALANCE); `304 when version matches`; `404 unknown slug`; `404 and no 500 on corrupt sections_json` (insertar por SQL una fila con `sections_json='{{{'` y GET → 404); `401 without token`; `per-slug seed adds new slugs but never overwrites` (editar dashboard a v7 por SQL + agregar un slug fantasma al invocar seedScreens con lista extendida → dashboard sigue v7 Y el nuevo slug existe). RED.
- [ ] **Step 2:** Implementar (referencia NeoVita `ScreenRepository`/`ScreenRoutes`, adaptando: por-slug seed + catch de deserialización → 404). Registrar en Routing tras `subscriptionRoutes()`. → GREEN + `:server:test` completo sin regresiones.
- [ ] **Step 3:** Commit `feat(server): screen_definitions + seed por-slug + GET /api/screens con 304 y 404-ante-corrupto`.

---

### Task 3: Renderer + Dashboard piloto (chrome nativo intacto)

**Interfaces (consume):** Task 1-2. **Produce:** `@Composable fun SduiRenderer(definition: ScreenDefinition, summary: FinanceSummary?, accounts: List<Account>, onNavigate: (Screen) -> Unit)` en `shared/.../ui/sdui/SduiRenderer.kt`.

- [ ] **Step 1:** `SduiRenderer` — itera `renderableSections(definition)` dentro de un `LazyColumn`: `HERO_BALANCE` → réplica del MinCard de Balance actual (mismos cálculos `assetsDebtsNet`/ingresos/egresos, parametrizado por summary+accounts — NO compartido con el fallback); `ACCOUNTS_SUMMARY` → réplica de la sección de cuentas actual (parametrizada); `CARD_ROW`/`CARD_LIST` → tarjetas Min* (título/subtítulo/badge; imageUrl se ignora en movi v1 si no hay loader de imágenes en el proyecto — verificar con grep si existe coil/kamel; si no existe, documentar en el reporte); `LINK_LIST` → filas estilo CardRow del Dashboard actual; `BANNER` → MinCard con title?/text y clickable si trae action. Acciones: mapear target→`Screen` con un `when` exhaustivo sobre los 15 targets (`"credits" -> Screen.Credits`, etc.); OPEN_URL → `LocalUriHandler`.
- [ ] **Step 2:** `DashboardScreen.kt` — mantener top bar + snackbar + bottom nav + sheets como CHROME (fuera del if); el `LazyColumn` actual completo se extrae RENOMBRADO a `DashboardFallback(...)` byte-idéntico (params = los estados que ya usa); estado nuevo: `var screenDef by remember { mutableStateOf(ScreenDefCache.dashboard) }` + en el `LaunchedEffect` existente: `runCatching { Repositories.wallets.getScreen("dashboard", screenDef?.version) }.onSuccess { it?.let { d -> screenDef = d; ScreenDefCache.dashboard = d } }` (silencioso al fallo — capa 2 en memoria: `object ScreenDefCache { var dashboard: ScreenDefinition? = null }` en `shared/.../data/`); cuerpo: `screenDef?.let { SduiRenderer(it, summary, accounts, onNavigate) } ?: DashboardFallback(...)`.
- [ ] **Step 3:** Compilar `:shared:compileDebugKotlinAndroid :shared:compileKotlinWasmJs` → verde. Commit `feat(ui): SduiRenderer movi + Dashboard servido por definición con fallback y chrome nativo`.

---

### Task 4: E2E — la promesa en movi

- [ ] Postgres local (DB movi ya existente — usar la de siempre vía `server/.env`); `./gradlew :server:run` background → health OK. Usuario desechable por curl → token.
- [ ] `GET /api/screens/dashboard` con token → 200, 6 secciones, version 1. Con `If-None-Match: 1` → 304.
- [ ] `psql <db movi> -c "UPDATE screen_definitions SET sections_json = REPLACE(sections_json,'Explora','Explora E2E'), version=2 WHERE slug='dashboard'"` → GET → version 2 con "Explora E2E" (SIN reiniciar server — cambio vivo al instante).
- [ ] Reiniciar server → GET → sigue v2 (seed por-slug no pisó). Corrupt-check: `UPDATE ... sections_json='{{{'` → GET → 404 (no 500) → restaurar con un UPDATE al JSON válido de v2 (guardarlo antes con SELECT).
- [ ] Cleanup (matar server, borrar throwaway y su rastro si aplica — la fila dashboard restaurada) + `git push -u origin feat/sdui-movi`. Reporte completo.
