<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- READ_SMS implies telephony hardware; mark it optional so the app stays
         installable on ChromeOS/tablets without telephony (SMS reader is a no-op there). -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:theme="@style/Theme.Movi">

        <activity
            android:name="com.jvillada.movi.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|keyboard"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Captura de los movimientos que las apps del banco publican como notificación
            (com.jvillada.movi.notificaciones.EscuchaDeNotificaciones). Existe porque el banco
            dejó de mandar SMS y la tarjeta Glim nunca los mandó.

            android:permission="...BIND_NOTIFICATION_LISTENER_SERVICE" es obligatorio: es lo que
            declara que SOLO el sistema puede enchufar este servicio. Sin eso, cualquier app del
            teléfono podría bindearlo. No se declara como <uses-permission>: es de firma, la tiene
            el sistema, y pedirla no hace nada.

            El servicio queda declarado pero INERTE hasta que el dueño lo habilite a mano en
            Ajustes → Acceso a las notificaciones. Android no lo llama antes, y no hay diálogo que
            la app pueda lanzar: la sección «Captura en este teléfono» lleva a esa pantalla.
        -->
        <service
            android:name="com.jvillada.movi.notificaciones.EscuchaDeNotificaciones"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name="com.jvillada.movi.sms.SmsRealtimeReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
